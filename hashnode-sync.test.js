const { strict: assert } = require('node:assert')
const { describe, it } = require('node:test')

describe('All util methods', () => {
    it('should return if file exists', () => {
        
    })

    it('should return if token exists', () => {

    })

    it('should return if token works', () => {

    })

    it('should find all md files, parse them and store them in json file correctly', () => {

    })

    it('is file modified', () => {

    })
})